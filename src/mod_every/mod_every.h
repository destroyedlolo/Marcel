/* mod_every
 *
 * Repeating tasks
 *
 */

#ifndef MOD_EVERY_H
#define MOD_EVERY_H

#include "../Module.h"
#include "../Section.h"

	/* own structure */
struct module_every {
	struct Module module;

};

struct section_every {
	struct Section section;
};
#endif
